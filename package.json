{
  "name": "erp-ecosystem",
  "version": "1.0.0",
  "private": true,
  "description": "ERP Ecosystem - Open-core Enterprise Resource Planning Platform",
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "prepare": "husky || echo husky skipped",
    "setup": "node scripts/setup.js",
    "build": "turbo run build",
    "dev": "turbo run dev --concurrency=40",
    "dev:full": "turbo run dev --concurrency=40",
    "lint": "turbo run lint",
    "test": "turbo run test",
    "test:e2e": "turbo run test:e2e",
    "typecheck": "turbo run typecheck",
    "clean": "node scripts/clean.js",
    "format": "prettier --write \"**/*.{ts,tsx,js,jsx,json,md}\"",
    "db:generate": "turbo run db:generate",
    "db:push": "turbo run db:push",
    "db:push:full": "node scripts/db-push-full.js",
    "db:push:business": "node scripts/db-push-business.js",
    "db:seed": "turbo run db:seed",
    "db:seed:full": "node scripts/db-push-full.js --seed-accounting",
    "dev:business": "node scripts/run-business.js",
    "dev:business:one": "node scripts/run-business-oneport.js",
    "docker:up": "docker compose up -d",
    "docker:down": "docker compose down",
    "db:migrate": "turbo run db:generate && turbo run db:push"
  },
  "lint-staged": {
    "*.{ts,tsx}": ["eslint --fix", "prettier --write"],
    "*.{json,md,yml,yaml}": ["prettier --write"]
  },
  "dependencies": {
    "http-proxy": "^1.18.1"
  },
  "devDependencies": {
    "@commitlint/cli": "^19.0.0",
    "@commitlint/config-conventional": "^19.0.0",
    "husky": "^9.0.0",
    "lint-staged": "^15.2.0",
    "turbo": "^2.0.0",
    "prettier": "^3.2.0"
  },
  "engines": {
    "node": ">=20.0.0",
    "npm": ">=10.0.0"
  },
  "packageManager": "npm@10.8.0"
}
